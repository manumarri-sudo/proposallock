{
  "name": "proposallock",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo 'No build step needed for Bun'"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
